# Receive mail
set folder = "imaps://petr.stastny01@imap.gmail.com:993"
set spoolfile = "+INBOX"

# Send mail
set smtp_url = "smtp://petr.stastny01@smtp.gmail.com:587"
set record = "+sent"
set from = "petr.stastny01@gmail.com"
set realname = "Petr Šťastný"

# Misc
set trash = "+trash"
set postponed = "+draft"

mailboxes =INBOX =unread =sent =trash =draft =spam

# Passwords
#set smtp_pass = "mypassword"
#set imap_pass = "mypassword"

# Cache
set message_cachedir = "~/data/.neomutt-cache/"

# Colors
source colors.muttrc
